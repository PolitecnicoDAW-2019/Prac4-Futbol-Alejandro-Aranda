class Player {
  constructor({ id, name, alias, club, birthdate, selectPosition, idPosition, picture }) {
    this.id = id;
    this.name = name;
    this.alias = alias;
    this.club = club;
    this.birthdate = birthdate;
    this.selectPosition = selectPosition;
    this.picture = picture;
    this.idPosition = idPosition;
  }
}
