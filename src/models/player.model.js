class Player {
  constructor({ id, name, alias, club, birthdate, selectPosition, idPosition, namePicture }) {
    this.id = id;
    this.name = name;
    this.alias = alias;
    this.club = club;
    this.birthdate = birthdate;
    this.selectPosition = selectPosition;
    this.idPosition = idPosition;
    this.namePicture = namePicture;
  }
}
